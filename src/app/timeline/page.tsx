import { TimelineGate } from "@/components/TimelineGate";

export const metadata = { title: "个人发展时间轴", robots: { index: false, follow: false } };

export default function TimelinePage() { return <TimelineGate />; }
